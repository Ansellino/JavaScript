// Contoh String

const currentYear = new Date().getFullYear();
const text = `Sekarang adalah tahun ${currentYear}.`;

console.log(text);

// Contoh Number

const result = 50 / 0;
console.log(result); // output: Infinity

result = Number("Dicoding");
console.log(result); // Output : Nan

// Contoh Boolean
